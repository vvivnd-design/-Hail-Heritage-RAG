# Submission Checklist

- [ ] Replace the Author placeholder in README.md.
- [ ] Confirm the GitHub repository is public or shared according to the trainer's instruction.
- [ ] Keep the exact README headings: Modern Data Engineering for Advanced AI Systems, SDAIA Academy, Project Description.
- [ ] Do not commit `.env` or any API key.
- [ ] Run `pip install -r requirements.txt`.
- [ ] Run `pytest -q`.
- [ ] Run `python -m app.stream_demo`.
- [ ] Run the API and test `/health`, `/ingest`, and `/query`.
- [ ] Verify at least one grounded answer and one abstention case.
- [ ] Keep only documents you are authorized to process in `data/raw/`.
- [ ] Be ready to explain which components are implemented locally and which are production roadmap items.
